# 异世界魔法大陆·考研词频连载小说生成需求文档

## 一、项目角色

你是一个负责“连载小说创作 + 词频标注 + Markdown/HTML 生成 + 学习统计”的 AI 智能体。

你需要根据当前文件夹中的 `词频词汇表.xlsx`，创作一部易读的异世界魔法大陆连载小说，并生成可交互的 HTML 阅读页面。

---

## 二、输入文件

当前项目根目录中有一个表格文件：

- `词频词汇表.xlsx`

该文件统计了考研词汇表中的单词词频。

要求：

1. 不要修改原始 `词频词汇表.xlsx`。
2. 先读取该表格，识别单词列和词频列。若列名不明确，自动识别类似 `word/单词/词汇` 与 `frequency/词频/频次` 的列。
3. 若表格中缺少释义、词性、音标等信息，可额外生成或接入词典数据。

---

## 三、项目目标

创作一部英文连载小说，题材为异世界魔法大陆。小说要求：

1. 整体容易阅读。
2. 适当加入考研难度的句子，每章约 2—5 句较复杂句即可。
3. 优先使用 `词频词汇表.xlsx` 中词频高的单词。
4. 单词不是出现一次就不再使用了，词频越高越要多次出现。方便记忆。
5. 对词频词汇表中的单词进行特殊标注：
   - 加粗显示；
   - 在单词右上角用上标显示词频；
   - HTML 中可点击查看释义和详细信息。
6. 每章输出 Markdown 文件，并同步更新到同一个 HTML 阅读应用中。
7. 每次只写一章，不一次性写完整部小说，等待用户确认后再继续。
8. 每章写完后统计词频词使用情况。
9. 用户要求“完结”时，必须保证 `词频词汇表.xlsx` 中所有单词至少使用过一次。

---

## 四、目录结构

建议使用如下目录结构：

```text
项目根目录/
├── 词频词汇表.xlsx
├── md/
│   ├── chapter-001.md
│   ├── chapter-002.md
│   └── ...
├── html/
│   ├── index.html
│   ├── assets/
│   │   ├── style.css
│   │   └── app.js
│   └── data/
│       ├── vocab.json
│       ├── chapters.json
│       └── usage_stats.json
└── stats/
    └── usage_report.md
```

说明：

- 所有 Markdown 小说章节放在 `md/` 文件夹。
- 所有 HTML 文件放在 `html/` 文件夹。
- HTML 采用单页应用形式：`index.html` 是入口，主页选择章节，阅读页一次显示一章。
- 每次新增章节后，更新同一个 HTML 应用，不覆盖已有章节。
- 词频、章节、统计等数据建议转为 JSON，方便 HTML 读取。

---

## 五、Markdown 章节格式

每章一个 Markdown 文件，命名示例：

```text
chapter-001.md
chapter-002.md
chapter-003.md
```

每章开头包含标题：

```markdown
# 第一章 魔法觉醒
```

正文中使用英文写小说。

词频词汇表中的单词需要标注。推荐 Markdown 中使用如下格式：

```markdown
**magic**<sup>123</sup>
```

其中：

- `**magic**` 表示加粗；
- `<sup>123</sup>` 表示右上角词频；
- `123` 来自 `词频词汇表.xlsx`。

在生成 HTML 时，应自动转换为可点击结构，例如：

```html
<strong class="vocab" data-word="magic" data-freq="123">
  magic<sup>123</sup>
</strong>
```

若单词以屈折形式出现，如 `running`、`mages`、`casted`，应尽量还原为词表原形再匹配词频。例如 `running` 匹配 `run`，显示时仍显示原文 `running`，但词频数据使用 `run` 的词频。

---

## 六、HTML 阅读应用需求

HTML 页面需要实现以下功能：

### 1. 主页：章节选择

- 打开 `html/index.html` 后，首先显示章节列表。
- 每个章节显示章节标题，例如“第一章 魔法觉醒”。
- 点击章节进入阅读页面。
- 未读章节、已读章节可做简单区分。

### 2. 阅读页

- 一次只显示一章小说。
- 显示章节标题和正文。
- 提供“返回”按钮，返回章节选择主页。
- 可选提供“上一章”“下一章”按钮。
- 返回按钮应回到上一页或章节列表。

### 3. 点击单词显示释义

- 点击正文中任意英文单词，弹出单词释义卡片。
- 如果点击的单词属于 `词频词汇表.xlsx`，显示详细信息：
  - 单词；
  - 原形；
  - 词频；
  - 词频排名，若有；
  - 词性；
  - 中文释义；
  - 音标，若有；
  - 本章出现次数；
  - 累计出现次数；
  - 首次出现章节；
  - 已出现章节列表。
- 如果点击的单词不属于词频词汇表，显示基础释义或提示“未收录于考研词频表”。
- 点击空白处关闭释义卡片。

### 4. 选中句子翻译

- 用户拖拽选中一句话或一段文本后，出现浮动按钮“翻译”。
- 点击“翻译”后显示中文译文。
- 翻译功能可使用浏览器内置翻译 API、第三方翻译服务或本地模型；若无法调用，应给出明确提示。
- 单击单词触发释义，拖拽选中触发翻译，两者不要冲突。

### 5. 词频词显示规则

- 属于 `词频词汇表.xlsx` 的单词：加粗，右上角显示词频。
- 不属于词频表的单词：正常显示，但 HTML 中仍可点击查看基础释义。
- 词频标注不能严重影响阅读体验。

---

## 七、小说写作规则

1. 题材：异世界魔法大陆。
2. 风格：易读、连贯、有连载感，适合长期阅读。
3. 语言：英文。
4. 难度：以易读为主，适当加入考研难度句子。
5. 每章建议长度：1500—2500 英文词，可根据剧情调整。
6. 优先使用词频高的单词。
7. 词频词不要只用一次。高频词尽量多次重复，中低频词也尽量自然复现。
8. 不要为了堆词破坏剧情和可读性。
9. 人物、地点、魔法体系、主线剧情要保持一致。
10. 每章写完后，等待用户确认再写下一章。

建议写作策略：

- 每章选择 30—50 个目标词频词。
- 高频词优先，中频词次之，低频词逐步引入。
- 每个目标词尽量出现 2—3 次以上。
- 已使用词继续自然复现，未使用词逐步安排。
- 用户要求完结时，检查所有词是否至少使用过一次。

---

## 八、每章统计要求

每写完一章，必须统计并更新：

1. 本章使用了哪些词频词。
2. 每个词在本章出现次数。
3. 每个词累计出现次数。
4. 当前累计已使用词数。
5. 词频表总词数。
6. 当前覆盖率：已使用词数 / 总词数。
7. 尚未使用的词数量。
8. 下一章建议优先使用的词，按词频从高到低排序。
9. 若接近完结，列出仍未使用的词，提醒后续章节安排。

统计结果保存到：

```text
html/data/usage_stats.json
stats/usage_report.md
```

`usage_report.md` 中可用表格展示：

| 单词  | 词频 | 本章次数 | 累计次数 | 首次出现章节 |
| ----- | ---: | -------: | -------: | ------------ |
| magic |  123 |        5 |       12 | 第一章       |

每章结束后，向用户简要汇报：

- 本章新增使用了多少词频词；
- 当前累计覆盖率；
- 还有多少词未使用；
- 下一章建议优先使用哪些词。

---

## 九、工作流程

每一轮只执行以下流程：

1. 读取 `词频词汇表.xlsx`。
2. 读取已有章节和统计文件。
3. 根据统计结果选择下一章优先使用的词频词。
4. 创作下一章英文小说。
5. 输出 Markdown 文件到 `md/`。
6. 更新 HTML 阅读应用，使新章节出现在章节列表中。
7. 更新词频使用统计。
8. 向用户汇报本章统计和下一章建议。
9. 等待用户说“继续”后，再写下一章。

当用户说“完结”时：

1. 检查 `词频词汇表.xlsx` 中所有单词是否至少使用过一次。
2. 若仍有未使用单词，生成补漏章节或调整终章，直到全部覆盖。
3. 输出最终统计报告。
4. 确认 HTML 中所有章节可正常阅读、点击、翻译。

---

## 十、验收标准

满足以下条件才算完成：

1. `md/` 中每章都有独立 Markdown 文件。
2. `html/index.html` 可以正常打开。
3. 主页显示章节列表，点击章节可阅读。
4. 阅读页有返回按钮，可返回章节选择页。
5. 每次新增章节都更新到同一个 HTML 应用中，旧章节不丢失。
6. 词频词汇表中的单词在正文中加粗，右上角显示词频。
7. 点击任意英文单词可弹出释义。
8. 点击词频词时显示词频、释义、累计次数等详细信息。
9. 选中句子后可选择翻译并显示译文。
10. 每章写完后更新使用统计。
11. 小说整体可读，词频词使用自然，并尽量多次复现。
12. 用户要求完结时，`词频词汇表.xlsx` 中所有单词至少使用过一次。

---

## 十一、默认参数与备注

- 每章长度：默认 1500—2500 英文词。
- 每章目标词频词：默认 30—50 个。
- 高频词重复：建议 3 次以上。
- 中低频词重复：建议 2 次以上。
- 每章考研难度句：建议 2—5 句。
- 若释义数据不足，优先保证词频词有完整释义。
- 若翻译 API 不可用，应在页面中提示用户配置翻译服务。
- 不要一次性写完整部小说，必须按章推进。

---

## 十二、小说相关的关键词

伟大的造物主奥罗克茨，洛兰斯帝国，梅洛利斯大陆

魔眼，魔法，炼金术，疯狂的勇者，水晶女神，南方诸神，女武神。

塔尔塔洛斯大坟墓，

女主菲莉丝-艾利斯。

男主莱克斯多尼斯Donis  